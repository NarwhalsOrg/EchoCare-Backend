{
  "name": "healthcare-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "python app/main.py",
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "test": "pytest",
    "lint": "flake8 app tests"
  }
}